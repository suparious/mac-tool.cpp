# Bug Fix Test Configuration
# Tests for v1.2 fixes:
# 1. Decimal notation support
# 2. Loop count fix

# Test 1: Decimal notation
# -------------------------
# These should all work now

pause 0.150   # Should pause for 150ms (not 0ms)
keydown A 100
pause 0.5     # Should pause for 500ms
keydown B 100
pause 1.75    # Should pause for 1750ms
keydown C 100

# Test 2: Decimal ranges
pause 0.1-0.3   # Random pause between 100-300ms
keydown D 100
pause 0.05-0.15 # Random pause between 50-150ms
keydown E 100

# Test 3: Loop counter fix
# This should run exactly 3 times (not infinitely)
loop 3
    keydown T 50
    keydown E 50
    keydown S 50
    keydown T 50
    key SPACE
    pause 0.2
endloop

# Test 4: Nested loops
# Inner loop should run 2 times, outer loop 3 times
# Total "X" presses should be 6
loop 3
    keydown O 50
    pause 0.1
    loop 2
        keydown X 50
        pause 0.05
    endloop
    pause 0.2
endloop

# Test 5: Mix of features
loop 5
    pause 0.075-0.125  # Random 75-125ms pause
    keydown K 80-120    # Random 80-120ms keypress
endloop

# Done!
keydown ENTER 100
